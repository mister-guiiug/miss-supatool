# Miss Supatool n'a AUCUN secret de configuration : les projets Supabase et
# leurs clés sont saisis dans l'interface, à l'exécution, et ne sont jamais
# écrits sur disque. Ce fichier ne sert qu'à la télémétrie d'erreurs, qui est
# facultative.

# DSN Sentry (facultatif). Sans lui, les erreurs restent dans le journal local.
VITE_SENTRY_DSN=
