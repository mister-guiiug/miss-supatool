// Setup Vitest partagé : jest-dom, stub matchMedia, doubles de
// `virtual:pwa-register`, polyfill localStorage en mémoire — tout vient du
// socle (`@mister-guiiug/dev-pwa-config/vitest-setup`).
import '@mister-guiiug/dev-pwa-config/vitest-setup';
